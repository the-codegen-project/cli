{
  "name": "user-domain-eventcatalog",
  "version": "1.0.0",
  "description": "EventCatalog for user domain services",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "eventcatalog dev",
    "build": "eventcatalog build",
    "preview": "eventcatalog preview",
    "generate": "eventcatalog generate"
  },
  "dependencies": {
    "@eventcatalog/core": "^2.0.0"
  },
  "devDependencies": {
    "@eventcatalog/sdk": "^1.0.0"
  }
}
