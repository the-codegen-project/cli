{
  "name": "typescript-library",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "rimraf lib && npm run generate && tsc",
    "start": "node lib/index",
    "generate": "codegen generate"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "typescript": "5.4.5",
    "rimraf": "5.0.5",
    "@the-codegen-project/cli": "^0"
  }
}
