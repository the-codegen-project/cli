openapi: 3.0.0
info:
  title: Components
  version: 1.0.0
paths: {}
components:
  schemas:
    Pet:
      type: object
      properties:
        id:
          type: string
        breed:
          $ref: 'SCHEMAS_URL#/components/schemas/Breed'
