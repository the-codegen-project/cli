openapi: 3.0.0
info:
  title: Remote URL Test
  version: 1.0.0
paths:
  /pets:
    get:
      summary: List pets
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: 'COMPONENTS_URL#/components/schemas/Pet'
